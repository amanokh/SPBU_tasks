#include <bits/stdc++.h>
using namespace std;

struct robber{
    string name;
    int alive, s1, s2, s3, s4, s5, money, total;
};
int main(){

    freopen("name.txt", "r", stdin);

    for (int i = 0; i < 39; ++i) {
        cin >> list[i]->name;

    }

    return 0;
}

